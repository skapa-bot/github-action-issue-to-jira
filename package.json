{
  "name": "github-action-issue-to-jira",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "start": "node ./index.js",
    "test": "jest",
    "convert-action": "npx @vercel/ncc build && npx convert-action",
    "build": "ncc build index.js --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^1.8.2",
    "actions-toolkit": "^6.0.1",
    "jira-client": "^8.1.0"
  },
  "devDependencies": {
    "jest": "^28.1.0",
    "nock": "^12.0.0"
  }
}
